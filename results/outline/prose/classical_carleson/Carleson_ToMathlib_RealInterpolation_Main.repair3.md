You are correcting translations of verified Lean statements into mathematical English.
An independent checker found the English statements below unfaithful to the Lean. For each, write a corrected
statement in clear mathematical English (LaTeX between $...$), fixing the issue the checker raised. Be faithful to
the FULL Lean statement: keep every hypothesis, including the assumptions carried by instance arguments, stated in
words ("G is a finite abelian group", "μ is a probability measure", "X is a metric space"), and the exact
conclusion. Only instances with no mathematical content (decidability: `Decidable…`) may stay unstated. Never
replace an assumption by a stronger one, and state restrictive types (a natural number, a nonnegative real). For a
definition, name the setting its signature assumes. Keep citations and remarks only if the docstring or the Lean
supports them. Do not add
claims the Lean does not make; attribute anything beyond a definition's signature to its docstring, and when no
docstring, definition body (under "Project definitions") or Lean supports a description of an object, name it
instead of describing it. Give every variable its
type, introduce every symbol, and never use one letter for two things.

Reply with only a JSON object: {"<lean name>": {"statement": "..."}}
Use every Lean name exactly as given.

Namespaces open (prefixes omitted): MeasureTheory, TileStructure, Antichain.

## Project definitions these statements use
(What each project notion is. Any description of one in the English must agree with this.)

#### `ChoiceScale.spf_ch` (def)
Lean: `{α : Type u_1} → {ε : Type u_3} → {m : MeasurableSpace α} → {p p₀ q₀ p₁ q₁ : ENNReal} → {C₀ C₁ : NNReal} → {μ : Measure α} → [inst : ENorm ε] → {f : α → ε} → {t : ℝ} → t ∈ Set.Ioo (0 : ℝ) (1 : ℝ) → q₀ ≠ q₁ → (0 : ENNReal) < p₀ → (0 : ENNReal) < q₀ → (0 : ENNReal) < p₁ → (0 : ENNReal) < q₁ → p₀ ≠ p₁ → (0 : NNReal) < C₀ → (0 : NNReal) < C₁ → eLpNorm f p μ ∈ Set.Ioo (0 : ENNReal) ⊤ → ScaledPowerFunction`
Docstring: The particular choice of scaled power function that works in the proof of the real interpolation theorem.
Definition: `fun {α : Type u_1} {ε : Type u_3} {m : MeasurableSpace α} {p p₀ q₀ p₁ q₁ : ENNReal} {C₀ C₁ : NNReal} {μ : Measure α} [ENorm ε] {f : α → ε} {t : ℝ} (ht : t ∈ Set.Ioo (0 : ℝ) (1 : ℝ)) (hq₀q₁ : q₀ ≠ q₁) (hp₀ : (0 : ENNReal) < p₀) (hq₀ : (0 : ENNReal) < q₀) (hp₁ : (0 : ENNReal) < p₁) (hq₁ : (0 : ENNReal) < q₁) (hp₀p₁ : p₀ ≠ p₁) (hC₀ : (0 : NNReal) < C₀) (hC₁ : (0 : NNReal) < C₁) (hF : eLpNorm f p μ ∈ Set.Ioo (0 : ENNReal) ⊤) => { σ := ComputationsChoiceExponent.ζ p₀ q₀ p₁ q₁ t, d := ChoiceScale.d.{u_1, u_3} (α := α) (ε := ε) (m := m) (p := p) (p₀ := p₀) (q₀ := q₀) (p₁ := p₁) (q₁ := q₁) (C₀ := C₀) (C₁ := C₁) (μ := μ) (f := f), hd := ⋯, hd' := ⋯, hσ := ⋯ }`

#### `MeasureTheory.HasWeakType` (def)
Lean: `{α : Type u_1} → {α' : Type u_2} → {ε₁ : Type u_4} → {ε₂ : Type u_5} → {m : MeasurableSpace α} → {m_1 : MeasurableSpace α'} → [ENorm ε₁] → [ENorm ε₂] → [TopologicalSpace ε₁] → [TopologicalSpace ε₂] → ((α → ε₁) → α' → ε₂) → ENNReal → ENNReal → Measure α → Measure α' → ENNReal → Prop`
Docstring: An operator has weak type `(p, q)` if it is bounded as a map from `L^p` to weak `L^q`. `HasWeakType T p p' μ ν c` means that `T` has weak type `(p, p')` w.r.t. measures `μ`, `ν` and constant `c`.
Definition: `fun {α : Type u_1} {α' : Type u_2} {ε₁ : Type u_4} {ε₂ : Type u_5} {m : MeasurableSpace α} {m_1 : MeasurableSpace α'} [ENorm ε₁] [ENorm ε₂] [TopologicalSpace ε₁] [TopologicalSpace ε₂] (T : (α → ε₁) → α' → ε₂) (p p' : ENNReal) (μ : Measure α) (ν : Measure α') (c : ENNReal) => ∀ (f : α → ε₁), MemLp f p μ → AEStronglyMeasurable (T f) ν ∧ wnorm (T f) p' ν ≤ c * eLpNorm f p μ`

#### `MeasureTheory.PreservesAEStrongMeasurability` (def)
Lean: `{α : Type u_1} → {α' : Type u_2} → {m : MeasurableSpace α} → {m' : MeasurableSpace α'} → {μ : Measure α} → {ν : Measure α'} → {ε₁ : Type u_7} → {ε₂ : Type u_8} → [inst : TopologicalSpace ε₁] → [ESeminormedAddMonoid ε₁] → [TopologicalSpace ε₂] → ((α → ε₁) → α' → ε₂) → ENNReal → Prop`
Docstring: Proposition that expresses that the map `T` map between function spaces preserves AE strong measurability on L^p.
Definition: `fun {α : Type u_1} {α' : Type u_2} {m : MeasurableSpace α} {m' : MeasurableSpace α'} {μ : Measure α} {ν : Measure α'} {ε₁ : Type u_7} {ε₂ : Type u_8} [TopologicalSpace ε₁] [ESeminormedAddMonoid ε₁] [TopologicalSpace ε₂] (T : (α → ε₁) → α' → ε₂) (p : ENNReal) => ∀ ⦃f : α → ε₁⦄, MemLp f p μ → AEStronglyMeasurable (T f) ν`

#### `MeasureTheory.SubadditiveTrunc` (def)
Lean: `{α : Type u_1} → {α' : Type u_2} → {ε : Type u_3} → {m' : MeasurableSpace α'} → {ε₁ : Type u_7} → [inst : TopologicalSpace ε₁] → [ESeminormedAddMonoid ε₁] → [ENorm ε] → ((α → ε₁) → α' → ε) → ENNReal → (α → ε₁) → Measure α' → Prop`
Definition: `fun {α : Type u_1} {α' : Type u_2} {ε : Type u_3} {m' : MeasurableSpace α'} {ε₁ : Type u_7} [TopologicalSpace ε₁] [ESeminormedAddMonoid ε₁] [ENorm ε] (T : (α → ε₁) → α' → ε) (A : ENNReal) (f : α → ε₁) (ν : Measure α') => ∀ (a : ENNReal), (0 : ENNReal) < a → ∀ᵐ (y : α') ∂ν, ‖T (trunc f a + truncCompl f a) y‖ₑ ≤ A * (‖T (trunc f a) y‖ₑ + ‖T (truncCompl f a) y‖ₑ)`

#### `ScaledPowerFunction` (structure or class)
Lean: `Type`
Docstring: A ScaledPowerFunction is meant to represent a function of the form `t ↦ (t / d)^σ`, where `d` is strictly positive and either `σ > 0` or `σ < 0`.
Fields: `σ`, `d`, `0`

#### `ComputationsChoiceExponent.ζ` (def)
Lean: `ENNReal → ENNReal → ENNReal → ENNReal → ℝ → ℝ`
Definition: `fun (p₀ q₀ p₁ q₁ : ENNReal) (t : ℝ) => (((1 : ℝ) - t) * p₀⁻¹.toReal + t * p₁⁻¹.toReal) * (q₁⁻¹.toReal - q₀⁻¹.toReal) / ((((1 : ℝ) - t) * q₀⁻¹.toReal + t * q₁⁻¹.toReal) * (p₁⁻¹.toReal - p₀⁻¹.toReal))`

#### `ScaledPowerFunction.σ` (def)
Lean: `ScaledPowerFunction → ℝ`
Definition: `fun (self : ScaledPowerFunction) => self.1`

#### `ScaledPowerFunction.d` (def)
Lean: `ScaledPowerFunction → ENNReal`
Definition: `fun (self : ScaledPowerFunction) => self.2`

## Flagged translations

### `MeasureTheory.combine_estimates₁`
Lean (short): `(hA : 0 < A) (hp₀ : p₀ ∈ Set.Ioc 0 q₀) (hp₁ : p₁ ∈ Set.Ioc 0 q₁) (ht : t ∈ Set.Ioo 0 1) (hp₀p₁ : p₀ < p₁) (hq₀q₁ : q₀ ≠ q₁) (hp : p⁻¹ = (1 - t) * p₀⁻¹ + t * p₁⁻¹) (hq : q⁻¹ = (1 - t) * q₀⁻¹ + t * q₁⁻¹) (hf : MemLp f p μ) (hT : SubadditiveTrunc T (↑A) f ν) (h₁T : HasWeakType T p₁ q₁ μ ν ↑C₁) (h₀T : HasWeakType T p₀ q₀ μ ν ↑C₀) (h₂T : PreservesAEStrongMeasurability T p) (hC₀ : 0 < C₀) (hC₁ : 0 < C₁) (hF : eLpNorm f p μ ∈ Set.Ioo 0 ⊤) (hspf : spf = ChoiceScale.spf_ch ⋯ hq₀q₁ ⋯ ⋯ ⋯ ⋯ ⋯ hC₀ hC₁ hF) : eLpNorm (T f) q ν ≤ ENNReal.ofReal (2 * ↑A) * q ^ q⁻¹.toReal * ((if q₁ < ⊤ then 1 else 0) * ENNReal.ofReal |q.toReal - q₁.toReal|⁻¹ + (if q₀ < ⊤ then 1 else 0) * ENNReal.ofReal |q.toReal - q₀.toReal|⁻¹) ^ q⁻¹.toReal * ↑C₀ ^ (1 - t).toReal * ↑C₁ ^ t.toReal * eLpNorm f p μ`
Lean (full): `∀ {α : Type u_1} {α' : Type u_2} {E₁ : Type u_4} {E₂ : Type u_5} {m : MeasurableSpace α} {m' : MeasurableSpace α'} {p q p₀ q₀ p₁ q₁ : ENNReal} {C₀ C₁ : NNReal} {μ : Measure α} {ν : Measure α'} {f : α → E₁} {t : ENNReal} {T : (α → E₁) → α' → E₂} {A : NNReal}, (0 : NNReal) < A → ∀ [inst : TopologicalSpace E₁] [inst_1 : ESeminormedAddMonoid E₁] [inst_2 : TopologicalSpace E₂] [inst_3 : ContinuousENorm E₂] {spf : ScaledPowerFunction} (hp₀ : p₀ ∈ Set.Ioc (0 : ENNReal) q₀) (hp₁ : p₁ ∈ Set.Ioc (0 : ENNReal) q₁) (ht : t ∈ Set.Ioo (0 : ENNReal) (1 : ENNReal)) (hp₀p₁ : p₀ < p₁) (hq₀q₁ : q₀ ≠ q₁), p⁻¹ = ((1 : ENNReal) - t) * p₀⁻¹ + t * p₁⁻¹ → q⁻¹ = ((1 : ENNReal) - t) * q₀⁻¹ + t * q₁⁻¹ → MemLp f p μ → SubadditiveTrunc T (↑A) f ν → HasWeakType T p₁ q₁ μ ν ↑C₁ → HasWeakType T p₀ q₀ μ ν ↑C₀ → PreservesAEStrongMeasurability (m := m) (m' := m') (μ := μ) (ν := ν) T p → ∀ (hC₀ : (0 : NNReal) < C₀) (hC₁ : (0 : NNReal) < C₁) (hF : eLpNorm f p μ ∈ Set.Ioo (0 : ENNReal) ⊤), spf = ChoiceScale.spf_ch ⋯ hq₀q₁ ⋯ ⋯ ⋯ ⋯ ⋯ hC₀ hC₁ hF → eLpNorm (T f) q ν ≤ ENNReal.ofReal ((2 : ℝ) * ↑A) * q ^ q⁻¹.toReal * ((if q₁ < ⊤ then (1 : ENNReal) else (0 : ENNReal)) * ENNReal.ofReal |q.toReal - q₁.toReal|⁻¹ + (if q₀ < ⊤ then (1 : ENNReal) else (0 : ENNReal)) * ENNReal.ofReal |q.toReal - q₀.toReal|⁻¹) ^ q⁻¹.toReal * ↑C₀ ^ ((1 : ENNReal) - t).toReal * ↑C₁ ^ t.toReal * eLpNorm f p μ`
Previous English: Let $\alpha$ and $\alpha'$ be measurable spaces with measures $\mu$ on $\alpha$ and $\nu$ on $\alpha'$, let $E_1$ be a topological space carrying an extended-seminormed additive monoid structure ($\mathrm{ESeminormedAddMonoid}$), and let $E_2$ be a topological space with a continuous extended norm ($\mathrm{ContinuousENorm}$). Let $T$ map functions $\alpha \to E_1$ to functions $\alpha' \to E_2$. Let $p,q,p_0,q_0,p_1,q_1,t \in [0,\infty]$, and let $A, C_0, C_1$ be nonnegative reals with $A > 0$, $C_0 > 0$, $C_1 > 0$. Assume $p_0 \in (0, q_0]$, $p_1 \in (0, q_1]$, $t \in (0,1)$, $p_0 < p_1$, $q_0 \ne q_1$, $p^{-1} = (1-t)p_0^{-1} + t p_1^{-1}$ and $q^{-1} = (1-t)q_0^{-1} + t q_1^{-1}$. Let $f : \alpha \to E_1$ with $f \in L^p(\mu)$ and $0 < \|f\|_{L^p(\mu)} < \infty$; suppose $T$ is subadditive under truncation of $f$ with constant $A$ with respect to $\nu$ ($\mathrm{SubadditiveTrunc}\ T\ A\ f\ \nu$), $T$ has weak type $(p_1, q_1)$ with constant $C_1$ and weak type $(p_0, q_0)$ with constant $C_0$ (from $\mu$ to $\nu$), and $T$ preserves a.e. strong measurability for $L^p$ functions (from $\mu$ to $\nu$). Let $\mathrm{spf}$ be a scaled power function equal to $\mathrm{ChoiceScale.spf\_ch}(\dots)$ built from these data. Then $$\|T f\|_{L^q(\nu)} \le 2A \cdot q^{q^{-1}} \cdot \Big(\mathbf{1}[q_1 < \infty]\,|q - q_1|^{-1} + \mathbf{1}[q_0 < \infty]\,|q - q_0|^{-1}\Big)^{q^{-1}} \cdot C_0^{1-t}\, C_1^{t}\, \|f\|_{L^p(\mu)}. Here all exponents $p, q, p_0, p_1, q_0, q_1, t$ lie in the extended nonnegative reals $[0,\infty]$, $C_0, C_1, A$ are nonnegative reals, $\mathbf{1}[\cdot]$ is $1$ if the condition holds and $0$ otherwise, real powers use the real numbers $q^{-1}$, $1-t$, $t$ etc. converted from $[0,\infty]$, and $|q-q_i|^{-1}$ is computed in the reals (with $0^{-1}=0$) and then viewed in $[0,\infty]$.
Checker's issue: The display math opened with $$ is never closed, so the following explanatory text is swallowed into the formula; statement otherwise matches.

### `MeasureTheory.combine_estimates₀`
Lean (short): `(hA : 0 < A) (hp₀ : p₀ ∈ Set.Ioc 0 q₀) (hp₁ : p₁ ∈ Set.Ioc 0 q₁) (ht : t ∈ Set.Ioo 0 1) (hp₀p₁ : p₀ < p₁) (hq₀q₁ : q₀ ≠ q₁) (hp : p⁻¹ = (1 - t) * p₀⁻¹ + t * p₁⁻¹) (hq : q⁻¹ = (1 - t) * q₀⁻¹ + t * q₁⁻¹) (hf : MemLp f p μ) (hT : SubadditiveTrunc T (↑A) f ν) (hC₀ : 0 < C₀) (hC₁ : 0 < C₁) (hF : eLpNorm f p μ ∈ Set.Ioo 0 ⊤) (hspf : spf = ChoiceScale.spf_ch ⋯ hq₀q₁ ⋯ ⋯ ⋯ ⋯ ⋯ hC₀ hC₁ hF) (h₁T : HasWeakType T p₁ q₁ μ ν ↑C₁) (h₀T : HasWeakType T p₀ q₀ μ ν ↑C₀) (h₂T : PreservesAEStrongMeasurability T p) : ∫⁻ (x : α'), ‖T f x‖ₑ ^ q.toReal ∂ν ≤ ENNReal.ofReal ((2 * ↑A) ^ q.toReal * q.toReal) * ((if q₁ < ⊤ then 1 else 0) * ENNReal.ofReal |q.toReal - q₁.toReal|⁻¹ + (if q₀ < ⊤ then 1 else 0) * ENNReal.ofReal |q.toReal - q₀.toReal|⁻¹) * ↑C₀ ^ ((1 - t).toReal * q.toReal) * ↑C₁ ^ (t.toReal * q.toReal) * eLpNorm f p μ ^ q.toReal`
Lean (full): `∀ {α : Type u_1} {α' : Type u_2} {E₁ : Type u_4} {E₂ : Type u_5} {m : MeasurableSpace α} {m' : MeasurableSpace α'} {p q p₀ q₀ p₁ q₁ : ENNReal} {C₀ C₁ : NNReal} {μ : Measure α} {ν : Measure α'} {f : α → E₁} {t : ENNReal} {T : (α → E₁) → α' → E₂} {A : NNReal}, (0 : NNReal) < A → ∀ [inst : TopologicalSpace E₁] [inst_1 : ESeminormedAddMonoid E₁] [inst_2 : TopologicalSpace E₂] [inst_3 : ContinuousENorm E₂] {spf : ScaledPowerFunction} (hp₀ : p₀ ∈ Set.Ioc (0 : ENNReal) q₀) (hp₁ : p₁ ∈ Set.Ioc (0 : ENNReal) q₁) (ht : t ∈ Set.Ioo (0 : ENNReal) (1 : ENNReal)) (hp₀p₁ : p₀ < p₁) (hq₀q₁ : q₀ ≠ q₁), p⁻¹ = ((1 : ENNReal) - t) * p₀⁻¹ + t * p₁⁻¹ → q⁻¹ = ((1 : ENNReal) - t) * q₀⁻¹ + t * q₁⁻¹ → MemLp f p μ → SubadditiveTrunc T (↑A) f ν → ∀ (hC₀ : (0 : NNReal) < C₀) (hC₁ : (0 : NNReal) < C₁) (hF : eLpNorm f p μ ∈ Set.Ioo (0 : ENNReal) ⊤), spf = ChoiceScale.spf_ch ⋯ hq₀q₁ ⋯ ⋯ ⋯ ⋯ ⋯ hC₀ hC₁ hF → HasWeakType T p₁ q₁ μ ν ↑C₁ → HasWeakType T p₀ q₀ μ ν ↑C₀ → PreservesAEStrongMeasurability (m := m) (m' := m') (μ := μ) (ν := ν) T p → ∫⁻ (x : α'), ‖T f x‖ₑ ^ q.toReal ∂ν ≤ ENNReal.ofReal (((2 : ℝ) * ↑A) ^ q.toReal * q.toReal) * ((if q₁ < ⊤ then (1 : ENNReal) else (0 : ENNReal)) * ENNReal.ofReal |q.toReal - q₁.toReal|⁻¹ + (if q₀ < ⊤ then (1 : ENNReal) else (0 : ENNReal)) * ENNReal.ofReal |q.toReal - q₀.toReal|⁻¹) * ↑C₀ ^ (((1 : ENNReal) - t).toReal * q.toReal) * ↑C₁ ^ (t.toReal * q.toReal) * eLpNorm f p μ ^ q.toReal`
Previous English: Let $\alpha$ and $\alpha'$ be measurable spaces with measures $\mu$ on $\alpha$ and $\nu$ on $\alpha'$, let $E_1$ be a topological space carrying an extended-seminormed additive monoid structure ($\mathrm{ESeminormedAddMonoid}$), and let $E_2$ be a topological space with a continuous extended norm ($\mathrm{ContinuousENorm}$). Let $T$ map functions $\alpha \to E_1$ to functions $\alpha' \to E_2$. Let $p,q,p_0,q_0,p_1,q_1,t \in [0,\infty]$, and let $A, C_0, C_1$ be nonnegative reals with $A > 0$, $C_0 > 0$, $C_1 > 0$. Assume $p_0 \in (0, q_0]$, $p_1 \in (0, q_1]$, $t \in (0,1)$, $p_0 < p_1$, $q_0 \ne q_1$, $p^{-1} = (1-t)p_0^{-1} + t p_1^{-1}$ and $q^{-1} = (1-t)q_0^{-1} + t q_1^{-1}$. Let $f : \alpha \to E_1$ with $f \in L^p(\mu)$ and $0 < \|f\|_{L^p(\mu)} < \infty$; suppose $T$ is subadditive under truncation of $f$ with constant $A$ with respect to $\nu$ ($\mathrm{SubadditiveTrunc}\ T\ A\ f\ \nu$), let $\mathrm{spf}$ be a scaled power function equal to $\mathrm{ChoiceScale.spf\_ch}(\dots)$ built from these data, and suppose $T$ has weak type $(p_1, q_1)$ with constant $C_1$ and weak type $(p_0, q_0)$ with constant $C_0$ (from $\mu$ to $\nu$), and $T$ preserves a.e. strong measurability for $L^p$ functions (from $\mu$ to $\nu$). Then $$\int^{-} \|T f(x)\|_e^{q}\, d\nu(x) \le (2A)^{q}\, q \cdot \Big(\mathbf{1}[q_1 < \infty]\,|q - q_1|^{-1} + \mathbf{1}[q_0 < \infty]\,|q - q_0|^{-1}\Big) \cdot C_0^{(1-t)q}\, C_1^{tq}\, \|f\|_{L^p(\mu)}^{q},$$ where the left side is the lower Lebesgue integral of the extended norm, $(2A)^q q$ is computed in the reals and then viewed in $[0,\infty]$, and all powers are real powers with $q$ (and $1-t$, $t$) converted to real numbers. Here all exponents $p, q, p_0, p_1, q_0, q_1, t$ lie in the extended nonnegative reals $[0,\infty]$, $C_0, C_1, A$ are nonnegative reals, $\mathbf{1}[\cdot]$ is $1$ if the condition holds and $0$ otherwise, real powers use the real numbers $q^{-1}$, $1-t$, $t$ etc. converted from $[0,\infty]$, and $|q-q_i|^{-1}$ is computed in the reals (with $0^{-1}=0$) and then viewed in $[0,\infty]$.
Checker's issue: Describes ∫⁻ (lintegral) as the 'lower Lebesgue integral', which misnames the operation; it is the Lebesgue integral of a [0,∞]-valued function.
