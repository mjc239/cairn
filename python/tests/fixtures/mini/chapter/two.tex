\begin{lemma}\label{later}\label{later-alias}
  \uses{def-widget}
  A lemma stated after it is used.
  \begin{equation}x = y\end{equation}
\end{lemma}
\begin{proof}\uses{missing-label} Trivial. \end{proof}

\begin{theorem}[Main]\label{main}\mathlibok
  \uses{def-widget}
\end{theorem}
\begin{proof}\uses{nice, later-alias} Done. \end{proof}
